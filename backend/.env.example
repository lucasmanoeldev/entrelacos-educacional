DEBUG=true
SECRET_KEY=local-change-this-before-deploy
ALLOWED_HOSTS=localhost,127.0.0.1
FRONTEND_URL=http://localhost:3000
CORS_ALLOWED_ORIGINS=http://localhost:3000
# Para teste temporário com DEBUG=false, permita SQLite explicitamente:
# ALLOW_SQLITE=true
# Para banco persistente em produção, use PostgreSQL:
# DATABASE_URL=postgresql://user:password@host:5432/database
NVIDIA_API_KEY=
NVIDIA_MODEL=deepseek-ai/deepseek-v4-flash-0731
# Para recuperação de senha em produção, configure SMTP:
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.example.com
# EMAIL_HOST_USER=
# EMAIL_HOST_PASSWORD=
# EMAIL_USE_TLS=true
# EMAIL_USE_SSL=false
# DEFAULT_FROM_EMAIL=contato@seudominio.com
